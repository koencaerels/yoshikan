{% extends 'partials/_master.twig' %}

{% block main %}
    {% if record|default %}
        <div class="content-wrapper p-10">
            <h1>{{ record|title }}</h1>
            <div>
                {{ record.content }}
            </div>
        </div>
    {% endif %}
{% endblock main %}
